#lang racket
(define (p) (p))
(define (test x y)
  (if (= x 0)
  0
  y))

(test 0 (p))

аппликативный порядок:
чтобы вычислить (р), для этого необходимо вычислить (р) - зацикливание

нормальный порядок:
не вычисляем операнды
х = 0
ответ: 0

